<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a new post</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>

<body
    class="bg-slate-200 dark:bg-gradient-to-br dark:from-gray-900 dark:to-gray-800 text-gray-900 dark:text-white min-h-screen flex flex-col ">

    <!-- Profile Header -->
    <header
        class="w-full bg-gray-100 dark:bg-gray-800/80 backdrop-blur-md p-4 flex justify-between items-center shadow-lg sticky top-0 z-50">
        <div class="flex items-center space-x-4">
            <img src="/images/uploads/<%=user? user.profilePhoto: 'default.jpg'%>"
            class="w-8 h-8 rounded-full" alt="profilePhoto">            <h1 class="text-2xl font-bold">Hello, <span id="username" class="text-blue-700 dark:text-blue-300">
                    <%#= user.username %>
                </span> 👋</h1>
        </div>

        <!-- Hamburger Menu for Small Screens -->
        <button id="menuToggle"
            class="sm:hidden bg-gray-200 dark:bg-gray-700 px-3 py-2 rounded-md transition-all duration-300 hover:bg-gray-300 dark:hover:bg-gray-600">
            <i class="fas fa-bars text-blue-600 dark:text-white"></i>
        </button>


        <div id="nav"
            class="fixed top-0 right-0 w-2/4 h-[100vh] bg-gray-100 md:dark:bg-transparent dark:bg-gray-800/80 backdrop-blur-md  transform translate-x-full transition-transform duration-300 sm:relative sm:translate-x-0 sm:w-auto sm:h-auto sm:flex sm:space-x-4  flex p-6 gap-3 md:flex-row  flex-col md:p-0">

            <!-- Close Button for Sliding Nav -->
            <button id="closeNav"
                class="absolute top-4 right-4 sm:hidden bg-red-500 text-white px-3 py-2 rounded-md transition-all duration-300 hover:bg-red-600">
                <i class="fas fa-times"></i>
            </button>

            <!-- Theme Toggle Button -->
            <button id="themeToggle"
                class="mt-16 sm:mt-0 bg-gray-200 dark:bg-gray-700 px-4 py-2 w-fit rounded-md transition-all duration-300 hover:bg-gray-300 dark:hover:bg-gray-600">
                <i id="themeIcon" class="fas fa-moon text-blue-600 dark:text-yellow-500"></i>
            </button>

            <!-- Profile Button -->
            <a href="/" class="mt-6 sm:mt-0">
                <button
                    class=" dark:text-white md:px-6 md:py-2 p-0 md:rounded-md md:shadow-md flex items-center space-x-2">
                    <span>Home</span>
                </button>
            </a>


            <!-- Logout Button -->
            <a href="/auth/logout" class="mt-6 sm:mt-0">
                <button
                    class="bg-red-500 hover:bg-red-600 text-white px-6 py-2 md:rounded-md md:shadow-md flex items-center space-x-2">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>Logout</span>
                </button>
            </a>

        </div>
    </header>

    <main class="antialiased container mx-auto px-4 py-8 flex-grow ">


        <!-- Create Post Section -->
        <section class="w-[60%] mx-auto bg-gray-50 dark:bg-gray-800/60 backdrop-blur-md rounded-xl shadow-xl p-6 mb-8">
            <h2 class="text-2xl font-bold mb-4 flex items-center space-x-2">
                <i class="fas fa-edit text-blue-600 dark:text-blue-400"></i>
                <span>Create a New Post</span>
            </h2>
            <form id="postForm" action="/post/create" method="post">
                <textarea name="postData" rows="4" required
                    class="w-full bg-white dark:bg-gray-700/50 text-gray-900 dark:text-white p-4 rounded-lg mb-4 border border-gray-300 dark:border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300"
                    placeholder="What's on your mind?"></textarea>
                <button type="submit"
                    class="w-full sm:w-auto bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 px-6 rounded-full shadow-md transition transform hover:scale-105 flex items-center justify-center space-x-2">
                    <i class="fas fa-paper-plane"></i>
                    <span>Post</span>
                </button>
            </form>
        </section>
    </main>

</body>

</html>